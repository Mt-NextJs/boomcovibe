interface LoginFormData {
    userId: string;
    password: number;
}

export interface JoinFormData {
    name: string;
    userId: string;
    password: number;
    confirmPassword: number;
    email: string;
}
